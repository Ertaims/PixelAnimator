很好，这个阶段做整理非常合适。现在功能已经长出来了，接下来要让代码也“像个能长期养大的项目”。我先说明：这次只做了只读审查，没有修改任何源码。

**当前观察**
项目已有基础模块划分：

- [src/app](/f:/PixelAnimator/src/app)：应用主循环、窗口、菜单、文件对话框、导入导出弹窗
- [src/core](/f:/PixelAnimator/src/core)：`Project`、`AppContext`
- [src/tools](/f:/PixelAnimator/src/tools)：绘图与框选工具
- [src/commands](/f:/PixelAnimator/src/commands)：编辑命令
- [src/io](/f:/PixelAnimator/src/io)：项目与图片读写
- [src/ui](/f:/PixelAnimator/src/ui)：菜单、窗口、状态栏
- [src/render](/f:/PixelAnimator/src/render)：目前为空

文件体量上，几个文件已经偏重：

- [App.cpp](/f:/PixelAnimator/src/app/App.cpp)：约 `2357` 行
- [RectSelectionTool.cpp](/f:/PixelAnimator/src/tools/RectSelectionTool.cpp)：约 `1526` 行
- [ProjectWindow_Canvas.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_Canvas.cpp)：约 `972` 行
- [ProjectWindow_Tools.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_Tools.cpp)：约 `832` 行
- [AppContext.cpp](/f:/PixelAnimator/src/core/AppContext.cpp)：约 `830` 行
- [ProjectWindow_ToolProperties.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_ToolProperties.cpp)：约 `818` 行

**主要整理方向**
我建议按“低风险拆分优先”的方式整理，不先动行为逻辑，只先搬职责和命名边界。

1. **建立 `render` 模块**

`render` 很适合承接这些内容：

- OpenGL 纹理创建、上传、销毁
- PNG 图标加载成纹理
- 画布像素上传
- 棋盘格背景绘制
- 选区蚂蚁线绘制
- 图层预览小方块绘制
- 通用 ImGui 图标按钮绘制辅助

建议新增：

- `src/render/Texture.h/.cpp`
- `src/render/CanvasTexture.h/.cpp`
- `src/render/IconTextureLoader.h/.cpp`
- `src/render/SelectionOverlayRenderer.h/.cpp`
- `src/render/LayerPreviewRenderer.h/.cpp`

第一阶段最适合先迁：

- [ProjectWindow.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow.cpp) 里的 `ensureCanvasTexture` / `uploadCanvasPixels`
- [ProjectWindow_Tools.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_Tools.cpp) 和 [ProjectWindow_ToolProperties.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_ToolProperties.cpp) 里的图标纹理加载
- [RectSelectionTool.cpp](/f:/PixelAnimator/src/tools/RectSelectionTool.cpp) 里的蚂蚁线绘制函数

2. **拆分 `App.cpp`**

[App.cpp](/f:/PixelAnimator/src/app/App.cpp) 现在混合了：

- SDL / ImGui 初始化
- 主循环
- 菜单绑定
- 文件对话框
- 项目会话管理
- 新建/打开/保存
- 导入导出
- 精灵图导入弹窗
- 精灵图导出弹窗
- 快捷键
- 最近项目

建议拆成：

- `App.cpp`：只保留生命周期、主循环、总调度
- `App_Sessions.cpp`：项目会话创建、关闭、切换
- `App_FileDialogs.cpp`：打开、保存、导入、导出对话框回调
- `App_Shortcuts.cpp`：快捷键处理
- `App_ImportExport.cpp`：导入导出执行逻辑
- `App_SpriteSheetPopups.cpp`：精灵图导入/导出弹窗
- `RecentProjectsStore.h/.cpp`：最近项目读写

目标：让 `App.cpp` 降到 `400-700` 行左右。

3. **拆分 `RectSelectionTool.cpp`**

[RectSelectionTool.cpp](/f:/PixelAnimator/src/tools/RectSelectionTool.cpp) 现在承担太多：

- 鼠标状态机
- 矩形/椭圆选区生成
- 套索/多边形套索
- 选区像素变换
- 八手柄缩放
- 选区叠加绘制

建议拆成：

- `SelectionGeometry.h/.cpp`
  - 矩形、椭圆、多边形、套索 mask 生成
- `SelectionTransform.h/.cpp`
  - 平移、缩放、翻转映射
- `SelectionOverlayRenderer.h/.cpp`
  - 选区轮廓、蚂蚁线、八手柄绘制，放到 `render`
- `RectSelectionTool.cpp`
  - 只保留交互状态机和对上述模块的调用

目标：`RectSelectionTool.cpp` 降到 `500-700` 行。

4. **整理 `ProjectWindow` 面板**

现在 [ProjectWindow.h](/f:/PixelAnimator/src/ui/windows/ProjectWindow.h) 状态很多，面板逻辑虽然已经分文件，但状态仍集中在一个类里。

建议拆出轻量组件：

- `ToolbarPanel.h/.cpp`
- `LayerPanel.h/.cpp`
- `TimelinePanel.h/.cpp`
- `PalettePanel.h/.cpp`
- `CanvasPanel.h/.cpp`

短期可以不立刻拆类，只先把 `LayerPanelState`、`ToolbarState` 等状态移动到对应 panel 文件/结构中。这样 `ProjectWindow.h` 会明显清爽。

目标：`ProjectWindow.h` 降低依赖，不再直接包含所有工具头。

5. **整理 `AppContext`**

[AppContext.h](/f:/PixelAnimator/src/core/AppContext.h) 现在包含：

- 当前项目状态
- 当前工具状态
- 视图状态
- 像素选区
- 时间轴多选/分组
- 撤销历史

建议拆出：

- `PixelSelection.h/.cpp`
  - 选区 mask、选区布尔运算、边界计算、移动/缩放 mask
- `FrameSelectionState.h/.cpp`
  - 时间轴帧多选、分组、移动后索引修正
- `UndoHistory.h/.cpp`
  - 当前的 undo entry 捕获、恢复、比较

`AppContext` 最终只做状态聚合和协调，不亲自实现所有算法。

6. **整理 `Project` 图层与合成逻辑**

[Project.cpp](/f:/PixelAnimator/src/core/Project.cpp) 目前还可以接受，但图层系统继续发展后会变重。

建议：

- `LayerCompositor.h/.cpp`
  - 从 `Project::composeFrame` 抽离图层合成
  - 可放 `render` 或 `core`
  - 如果只是像素合成算法，我更倾向放 `core/rendering` 或 `render`
- `LayerOperations.h/.cpp`
  - 合并图层、移动图层、图层像素操作

这里不急，排在 `App` 和 `Selection` 之后。

7. **注释规范**

当前注释整体是有帮助的，但有些地方偏长，有些地方重复描述“做了什么”。

建议统一成三类注释：

- 文件头：说明该文件负责什么，不超过 `5` 行
- 类型/函数注释：说明用途、关键约定、边界行为
- 复杂逻辑块注释：说明“为什么这样做”，不要逐行解释“怎么做”

特别建议保留中文注释，但避免过度说明显而易见的代码，例如 `width` 是宽度这种注释可以减少。

8. **命名与结构一致性**

建议统一：

- 成员变量使用 `m_xxx`
- 局部变量使用 `camelCase`
- 文件名按职责命名，不用过泛的名字
- 旧文件 `commands/new.h/.cpp` 建议改名为 `NewProjectCommand` 或确认是否仍需要
- `statusBar` 建议统一成 `StatusBar`

9. **CMake 整理**

当前 [CMakeLists.txt](/f:/PixelAnimator/CMakeLists.txt) 是手动维护 `SOURCES` 列表。整理时要记得新增 `render` 文件。

建议暂时继续手动列文件，避免 `GLOB` 带来不可见变更，但可以按模块分组：

- `APP_SOURCES`
- `CORE_SOURCES`
- `RENDER_SOURCES`
- `UI_SOURCES`
- `TOOL_SOURCES`
- `COMMAND_SOURCES`
- `IO_SOURCES`

**推荐执行顺序**
1. 新建 `render` 基础模块，迁移纹理加载与画布纹理上传。
2. 从 `RectSelectionTool.cpp` 拆出选区叠加绘制到 `render/SelectionOverlayRenderer`。
3. 从 `RectSelectionTool.cpp` 拆出选区几何和变换算法。
4. 拆分 `App.cpp`：先拆快捷键、文件对话框、导入导出弹窗。
5. 拆分图层面板：把 `LayerPanel` 从 `ProjectWindow_ToolProperties.cpp` 独立出来。
6. 拆分 `AppContext`：选区、帧分组、撤销历史分离。
7. 统一注释风格、命名风格和 CMake 分组。
8. 每一步都构建一次，必要时做一次手动回归测试。

**预期整改结果**
整理完成后，目标状态应该是：

- 单个 `.cpp` 尽量控制在 `300-700` 行，复杂工具最多不超过 `800` 行。
- `App` 只负责应用级调度，不再承载大段弹窗和导入导出细节。
- `ProjectWindow` 只负责组合面板，不再持有过多面板内部实现。
- `tools` 专注交互和像素修改，渲染辅助迁到 `render`。
- `render` 不持有业务状态，只接收数据并负责画出来。
- `core` 保持纯业务模型，尽量减少 ImGui / OpenGL 依赖。
- 注释短、准、解释关键意图。
- 之后继续加动画、图层、选区功能时，不会再把几个大文件越堆越厚。

我的建议是第一轮先从 `render` 和 `RectSelectionTool.cpp` 开始，因为收益最大、边界也最清楚。
-- -------------------------------------------------------------------------------------------------------------------
第一阶段：
第一阶段整理完成了，`render` 目录现在不是空壳了，已经开始承担渲染基础设施职责。

**完成内容**
新增了这几个文件：

- [Texture.h](/f:/PixelAnimator/src/render/Texture.h)
- [Texture.cpp](/f:/PixelAnimator/src/render/Texture.cpp)
- [CanvasTexture.h](/f:/PixelAnimator/src/render/CanvasTexture.h)
- [CanvasTexture.cpp](/f:/PixelAnimator/src/render/CanvasTexture.cpp)

它们现在负责：

- 图片文件加载为 OpenGL 纹理
- OpenGL 纹理安全释放
- 画布纹理创建、尺寸更新、像素上传
- 画布纹理使用 `GL_NEAREST`，保持像素画缩放硬边

**整理掉的重复代码**
把重复的纹理加载逻辑从这些地方抽走了：

- [App.cpp](/f:/PixelAnimator/src/app/App.cpp)
- [ProjectWindow_Tools.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_Tools.cpp)
- [ProjectWindow_Timeline.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_Timeline.cpp)
- [ProjectWindow_ToolProperties.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_ToolProperties.cpp)

同时把 [ProjectWindow.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow.cpp) 里的画布纹理 OpenGL 细节迁到了 `render::CanvasTexture`，`ProjectWindow` 现在只做调用。

**其他调整**
- [ProjectWindow.h](/f:/PixelAnimator/src/ui/windows/ProjectWindow.h) 里的 `CanvasTextureState` 已移除，改用 `render::CanvasTexture`。
- [ProjectWindow_Canvas.cpp](/f:/PixelAnimator/src/ui/windows/ProjectWindow_Canvas.cpp) 通过 `canvasTexture_.id()` 获取纹理。
- [CMakeLists.txt](/f:/PixelAnimator/CMakeLists.txt) 已加入新的 `render` 源文件。
- 顺手压短了画布纹理相关注释，避免简单包装函数还挂着很长的 OpenGL 说明。
-- -------------------------------------------------------------------------------------------------------------------
第二阶段：
第二阶段整理完成了，这次把选区叠加绘制从 `RectSelectionTool` 里抽到了 `render` 模块，代码边界更清楚了。

**完成内容**
新增：

- [SelectionOverlayRenderer.h](/f:/PixelAnimator/src/render/SelectionOverlayRenderer.h)
- [SelectionOverlayRenderer.cpp](/f:/PixelAnimator/src/render/SelectionOverlayRenderer.cpp)

现在 `render` 负责：

- 选区实线轮廓绘制
- 选区蚂蚁线绘制
- 八手柄中心点计算
- 八手柄绘制
- 多边形套索辅助线和顶点绘制

`RectSelectionTool` 现在只负责：

- 选区交互状态机
- 生成当前应该显示的 `displayMask`
- 根据当前状态决定显示颜色
- 调用 `render::...` 完成实际绘制

**整理效果**
- [RectSelectionTool.cpp](/f:/PixelAnimator/src/tools/RectSelectionTool.cpp) 从约 `1526` 行降到约 `1316` 行。
- 新的 [SelectionOverlayRenderer.cpp](/f:/PixelAnimator/src/render/SelectionOverlayRenderer.cpp) 约 `191` 行。
- [CMakeLists.txt](/f:/PixelAnimator/CMakeLists.txt) 已加入 `src/render/SelectionOverlayRenderer.cpp`。
-- -------------------------------------------------------------------------------------------------------------------
第三阶段：
第三阶段整理完成了，这次把 `RectSelectionTool` 里的选区几何和 mask 生成逻辑拆出来了，工具类又轻了一大截。

新增文件：

- [SelectionGeometry.h](/f:/PixelAnimator/src/tools/selection/SelectionGeometry.h)
- [SelectionGeometry.cpp](/f:/PixelAnimator/src/tools/selection/SelectionGeometry.cpp)

现在 `SelectionGeometry` 负责：

- 矩形选区写入 mask
- 椭圆选区写入 mask
- 任意 mask 的 `Replace/Add/Remove` 合并
- 套索路径补线
- 套索 mask 生成
- 多边形套索 mask 生成
- 点击起点闭合判断
- mask 是否为空判断

`RectSelectionTool` 现在只保留交互状态机和模式分发，例如用户当前是矩形、椭圆、套索还是多边形套索；具体几何算法交给 `selection::...`。

体量变化也比较明显：

- [RectSelectionTool.cpp](/f:/PixelAnimator/src/tools/RectSelectionTool.cpp) 现在约 `1041` 行
- [SelectionGeometry.cpp](/f:/PixelAnimator/src/tools/selection/SelectionGeometry.cpp) 约 `291` 行
- [SelectionGeometry.h](/f:/PixelAnimator/src/tools/selection/SelectionGeometry.h) 约 `37` 行
-- -------------------------------------------------------------------------------------------------------------------
第四阶段：
把右侧属性栏里最“胖”的图层 UI 单独拎出来了，结构清爽很多。

**本阶段完成**
- 新增独立图层面板类：`src/ui/windows/LayerPanel.h`
- 新增图层面板实现：`src/ui/windows/LayerPanel.cpp`
- `ProjectWindow` 不再直接维护图层面板状态，改为持有 `LayerPanel layerPanel_`
- `ProjectWindow_ToolProperties.cpp` 中原本大段图层 UI 被替换为：
```cpp
layerPanel_.render(*project, *context);
```
- 图层图标纹理加载、释放、多选、合并、重命名、透明度滑杆等逻辑全部迁移到 `LayerPanel`
- 更新 `CMakeLists.txt`，加入 `LayerPanel.cpp`

**整理效果**
- `ProjectWindow_ToolProperties.cpp` 现在只负责工具属性、图层面板入口和项目信息，不再混杂大量图层列表逻辑。
- 图层 UI 的状态集中到了 `LayerPanel::State`，更符合“单模块负责单职责”。
- 图层面板后续如果继续美化、加混合模式、图层缩略图、拖拽排序，也都可以集中改 `LayerPanel`，不会继续撑大 `ProjectWindow`。
-- -------------------------------------------------------------------------------------------------------------------
第五阶段：
这一阶段整理完成，我们把 `App` 里的导入/导出大块逻辑拆出来了。

**完成内容**
- 新增 `src/app/App_ImportExport.cpp`
- 从 `src/app/App.cpp` 迁移了：
  - `requestExportDialog`
  - `requestImportDialog`
  - `onExportDialogClosed`
  - `onImportDialogClosed`
  - `exportToPath`
  - `importFromPath`
  - `renderSpriteSheetImportPopup`
  - `renderSpriteSheetExportPopup`
  - `parseFrameListText`
  - `buildResolvedSpriteGroups`
- 清理了 `src/app/App.cpp` 中不再需要的导入/导出 include 和辅助函数。
- 更新 `CMakeLists.txt`，加入 `src/app/App_ImportExport.cpp`。

**整理效果**
- `src/app/App.cpp` 从约 `2695` 行降到 `1442` 行。
- 导入/导出逻辑集中到 `src/app/App_ImportExport.cpp`，后续优化 PNG 导入、精灵表切片、导出布局时不用再挤在主流程文件里。
- `App.cpp` 现在更偏向主流程、窗口、菜单、会话、保存/打开等核心调度。
-- -------------------------------------------------------------------------------------------------------------------
第六阶段：
这一阶段整理完成，我们把项目文件读写逻辑从 `App.cpp` 里拆出来了。

**完成内容**
- 新增 `src/app/App_FileIO.cpp`
- 迁移了以下职责：
  - 打开项目弹窗：`requestOpenProjectDialog`
  - 另存为弹窗：`requestSaveAsDialog`
  - 打开/保存弹窗回调：`onOpenDialogClosed`、`onSaveDialogClosed`
  - 保存项目：`saveProjectAs`、`saveActiveProject`、`saveActiveProjectAs`
  - 打开项目：`openProjectFromPath`
  - 打开项目后创建会话：`createSessionFromProject`
  - 最近项目：`addRecentProjectPath`、`refreshRecentProjectsMenu`、`loadRecentProjectPaths`、`saveRecentProjectPaths`
- 给 `App_FileIO.cpp` 里的路径辅助函数添加了中文注释：
  - 小写比较
  - 扩展名判断
  - 保存格式推断
  - 保存路径规范化
  - 从路径推导项目名
- 更新 `CMakeLists.txt`，加入 `src/app/App_FileIO.cpp`
- 清理 `src/app/App.cpp` 中不再需要的文件 IO include。

**整理效果**
- `src/app/App.cpp` 继续瘦身，从约 `1442` 行降到 `1056` 行。
- 文件读写逻辑集中到 `App_FileIO.cpp`，以后改保存格式、最近项目、打开路径校验，不需要再挤进主流程文件。
- 目前 `App` 相关文件职责更清楚：
  - `App.cpp`：主流程、窗口、事件、快捷键、编辑命令
  - `App_FileIO.cpp`：项目打开/保存/最近项目
  - `App_ImportExport.cpp`：PNG/精灵图导入导出
-- -------------------------------------------------------------------------------------------------------------------
第七阶段：
这一阶段整理完成，我们把编辑命令和撤销历史从 `App.cpp` 拆出来了。

**完成内容**
- 新增 `src/app/App_Edit.cpp`
- 迁移了：
  - `handleEditMenuShortcuts`
  - `executeCutSelection`
  - `executeCopySelection`
  - `executeDelete`
  - `executeRotate`
  - `executeFlip`
  - `executePasteSelection`
  - `renderUndoHistoryPopup`
- 清理了 `src/app/App.cpp` 中不再需要的命令头文件 include。
- 更新 `CMakeLists.txt`，加入 `src/app/App_Edit.cpp`。

**整理效果**
- `src/app/App.cpp` 从约 `1056` 行降到 `827` 行。
- 编辑逻辑集中到 `App_Edit.cpp`：
  - 撤销/重做快捷键
  - Cut/Copy/Paste
  - Delete
  - Rotate/Flip
  - Undo History 弹窗
- 现在 `App.cpp` 更接近真正的主流程文件，代码职责继续变清晰。
-- -------------------------------------------------------------------------------------------------------------------
第八阶段：
这一阶段整理完成，我们把“项目会话管理”从 `App.cpp` 拆出来了。

**完成内容**
- 新增 `src/app/App_Project.cpp`
- 迁移了：
  - `setActiveContext`
  - `findSessionIndexByContext`
  - `closeProjectByContext`
  - `closeAllProjects`
  - `refreshWindowLabels`
  - `handleProjectSwitchShortcut`
  - `renderNewProjectPopup`
  - `createSessionFromProject`
  - `createNewProject`
- `createSessionFromProject` 从 `App_FileIO.cpp` 移到 `App_Project.cpp`，职责更准确。
- 更新 `CMakeLists.txt`，加入 `src/app/App_Project.cpp`
- 清理了 `App.cpp` 里不再需要的 `cmath` include。

**整理效果**
- `src/app/App.cpp` 从约 `827` 行降到 `626` 行。
- 现在 App 拆分更清晰：
  - `App.cpp`：主循环、初始化、渲染调度、通用弹窗/快捷键
  - `App_Project.cpp`：项目会话、新建/关闭、多窗口切换
  - `App_FileIO.cpp`：打开/保存/最近项目
  - `App_ImportExport.cpp`：PNG/精灵图导入导出
  - `App_Edit.cpp`：编辑命令和撤销历史
-- -------------------------------------------------------------------------------------------------------------------
第九阶段：
这一阶段完成，我们按建议把“快捷键”和“通用弹窗/异步结果”拆出来了。

**完成内容**
- 新增 `src/app/App_Dialogs.cpp`
  - `pollDialogResults`
  - `renderErrorPopup`
  - `showError`

- 新增 `src/app/App_Shortcuts.cpp`
  - `handleFileMenuShortcuts`
  - `handleToolShortcuts`

- 更新 `CMakeLists.txt`
  - 加入 `src/app/App_Dialogs.cpp`
  - 加入 `src/app/App_Shortcuts.cpp`

- `App.cpp` 中保留了拖拽打开项目需要的轻量扩展名判断 helper，因为 `processEvents()` 仍在主流程文件里使用它。

**整理效果**
- `src/app/App.cpp` 从约 `626` 行降到 `419` 行。
- 现在职责更清楚：
  - `App.cpp`：初始化、主循环、事件、渲染调度、资源释放
  - `App_Dialogs.cpp`：异步弹窗结果轮询、错误弹窗
  - `App_Shortcuts.cpp`：File/Tool 全局快捷键
  - `App_Edit.cpp`：Edit 命令和 Undo History
  - `App_Project.cpp`：项目会话和多窗口管理
  - `App_FileIO.cpp`：打开/保存/最近项目
  - `App_ImportExport.cpp`：导入/导出
-- -------------------------------------------------------------------------------------------------------------------
