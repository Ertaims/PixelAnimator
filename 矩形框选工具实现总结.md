今天完成了矩形框选工具从功能实现到算法稳定化的一整轮优化，重点如下。

**今日工作总结**
1. 完成矩形框选工具完整交互链路
- 左键框选（`Replace`）  
- `Ctrl + 左键`并入选区（`Add`）  
- 右键减选（`Remove`）  
- 选区内部拖拽平移  
- 8 手柄缩放，`Ctrl` 等比  
- 与画笔/橡皮/填充联动：仅选区可编辑

2. 工程结构重构
- 将框选从 `ProjectWindow_Canvas` 拆分为独立工具类 `RectSelectionTool`
- 画布层只负责输入分发和绘制调用，降低耦合
- 每个项目窗口持有独立工具实例，避免多窗口串状态

3. 选区可视化增强
- 增加蚂蚁线（marching ants）动画边框
- 编辑过程中隐藏 8 手柄，避免视觉干扰

4. 关键 bug 修复
- 修复缩放后选区内部“不可编辑空洞”
- 修复平移时像素异常漂移/越界
- 实现平移/缩放实时预览

---

**论文重点：框选平移与缩放算法优化**

**1) 选区表示**
- 采用像素级二值掩码 `M(x,y) ∈ {0,1}` 表示任意形状选区，而非仅外接矩形。
- 优点：支持 `Add/Remove` 形成非规则选区，同时为后续变换提供统一约束条件。

**2) 缩放算法从“正向投影”改为“反向采样”**
- 旧方法（缺陷）：源到目标正向投影  
  每个源像素映射到目标坐标，放大时会出现落点稀疏，导致空洞。
- 新方法（优化）：目标到源反向采样  
  对目标区域每个像素 `(dx,dy)`，反算源坐标 `(sx,sy)`，再读取源数据。  
- 结果：目标区域全覆盖，不再出现框内“不可编辑洞”。

可写为：
\[
u=\frac{dx-dst_x}{dst_w-1},\quad
v=\frac{dy-dst_y}{dst_h-1}
\]
\[
sx=src_x+\mathrm{round}(u\cdot(src_w-1)),\quad
sy=src_y+\mathrm{round}(v\cdot(src_h-1))
\]
若 `M(sx,sy)=1`，则目标像素有效。

**3) 非破坏性连续变换（核心）**
- 问题：连续“缩小→放大”若每步都基于上一步结果，会累计采样误差，细节退化。
- 方案：引入变换源缓存  
  - `sourceFramePixels`：首次变换前原始像素  
  - `sourceSelectionMask`：对应原始选区掩码  
  - `sourceBounds`：原始外接框  
- 每一帧预览均从同一源缓存重采样到当前目标框，而不是链式重采样。
- 效果：大幅减少形状退化，满足“缩小再放大尽量恢复形状”的需求。

**4) 平移算法优化**
- 采用“剪切式平移（cut-move）”：
1. 清空源选区像素  
2. 将源选区像素按偏移 `(dx,dy)` 写入目标  
3. 越界自动裁剪
- 关键修复：平移开始时必须强制重建缓存（当前帧+当前选区），不能复用历史缩放缓存，否则会出现像素漂移与越框。

**5) 实时预览策略**
- 拖拽期间每帧重算预览图：
  - `preview = Transform(source, currentDragParam)`
- 松开鼠标后提交：
  - 更新帧像素
  - 更新选区掩码（平移或缩放后的掩码）
- 优点：交互连续、可控，无累计误差抖动。

---

**可用于论文的贡献点表述（可直接改写）**
1. 提出基于像素掩码的选区表达方式，统一支持布尔选区编辑与几何变换约束。  
2. 将缩放从正向投影改为目标反向采样，消除放大过程中的离散空洞问题。  
3. 设计非破坏性变换缓存机制，避免连续交互中的重采样误差累积。  
4. 建立平移/缩放实时预览流水线，实现“预览-提交一致性”并提升交互可解释性。  
5. 在工程层完成工具模块化重构，提高算法可维护性与多窗口状态隔离能力。